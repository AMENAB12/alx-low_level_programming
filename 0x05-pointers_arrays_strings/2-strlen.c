#include <stdio.h>

/**
 * _strlen - returns the length of a string
 * @ast: string to get length
 * Return: length of @str.
 */
size_t _strlen(const char *str)
{
	size_t length = 0;
	while (*astr++)
		length++;
	return (length);
}
