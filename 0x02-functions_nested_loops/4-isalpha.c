#include "main.h"

/**
 * _isalpha - Write a function that checks for alphabetic character.
 * @c: the char
 * Return: 1 if is lower or upper, 0 otherwise
 */

int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (C >= 'A' && <= 'Z'))
		return (1);
	else
		return (0);
}

